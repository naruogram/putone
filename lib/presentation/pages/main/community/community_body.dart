import 'package:flutter/material.dart';

class CommunityBody extends StatelessWidget {
  CommunityBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('communityScreen'),
    );
  }
}
